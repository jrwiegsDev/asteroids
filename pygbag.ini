; pygbag build config. pygbag skips venv/ by default but not uv's .venv/,
; which would otherwise get packed into the browser build.
[DEPENDENCIES]
ignoreDirs = ["/.venv", "/__pycache__"]
ignoreFiles = []
